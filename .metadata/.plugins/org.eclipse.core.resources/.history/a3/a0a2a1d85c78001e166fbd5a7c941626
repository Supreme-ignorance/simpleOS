#include "app_service.h"
#include "device_driver.h"

int ChangeApp(int index)
{
	int result = 0;

	curAppNum = (curAppNum + 1) % 2;
	int result = ChangeApp(curAppNum);
	Uart_Printf("\n [INFO] CHANGE APP ==> APP%d, result : %s", curAppNum, result == 1 ? "true":"false");

	return result;
}
